#include "main.h"
#include <stdio.h>

/**
 * _printf - takes in a const char and format pointer and an interger age
 * @format - writes a format function
 * @name - name of the user
 * @age - is the age of the user
 */
int _printf(const char *format, int age)
{

	char name[] = "Qhawe";

	int age = 22;

	format_output(name, age);
		if (NULL == 0)

	  printf("Hello,this format is n %s i am %d years old.\n", format, age);

	 
}
